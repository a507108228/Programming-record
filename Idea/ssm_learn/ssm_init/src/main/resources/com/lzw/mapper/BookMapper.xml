<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.lzw.mapper.BookMapper">

    <!--// 增加一本书-->
    <!--int addBook(Books book);-->
    <insert id="addBook" parameterType="com.lzw.bean.Books">
        insert into books(bookName, bookCounts, detail)
        VALUES (#{bookName}, #{bookCounts}, #{detail})
    </insert>


    <!--// 根据id删除一本书-->
    <!--int deleteBookById(int id);-->
    <delete id="deleteBookById" parameterType="com.lzw.bean.Books">
        delete from books where bookID=#{bookID}
    </delete>


    <!--// 更新一本书-->
    <!--int updateBook(Books book);-->
    <update id="updateBook" parameterType="com.lzw.bean.Books">
        update books
        set bookName = #{bookName},bookCounts = #{bookCounts}, detail = #{detail}
            where bookID = #{bookID};
    </update>


    <!--// 根据id查询一本书-->
    <!--Books queryBookById(int id);-->
    <select id="queryBookById" resultType="com.lzw.bean.Books">
        select *
        from books
        where bookID = #{bookID};
    </select>


    <!--// 查询全部book，返回list集合-->
    <!--List<Books> queryAllBook();-->
    <select id="queryAllBook" resultType="com.lzw.bean.Books">
        select *from books
    </select>

    <select id="queryBookByName" resultType="com.lzw.bean.Books">
        select *from books where bookName = #{bookName}
    </select>



</mapper>
